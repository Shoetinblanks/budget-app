services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: budget-app
    container_name: budget-app-web
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env
    # The Cloudflare Tunnel will route to this port 3000
